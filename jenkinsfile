pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                go build
            }
        }

         stage('Run') {
            steps {
                sh './goslow'
            }
         }
    }
}